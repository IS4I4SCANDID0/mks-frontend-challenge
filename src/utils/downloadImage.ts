// import fs from "fs";
// import axios from "axios";

// export const downloadImage = async (url: string, imagePath: string): Promise<void> => {
//   const res = await axios.get(url, { responseType: "arraybuffer" });

//   fs.writeFileSync(imagePath, res.data)
// }